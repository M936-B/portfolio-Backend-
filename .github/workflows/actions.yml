# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: integration

on:
  pull_request:
  push:
    branches:
      - ft-new-github-action
      - develop


jobs:
  build:

    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [14.x, 16.x, 18.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - uses: actions/checkout@v3
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    - run: npm i

  unit-tests:

    runs-on: ubuntu-latest

    env:
      PORT: 4000
      SALT: 10
      DATABASE_URI: mongodb+srv://bryanAtlas:kigalipassword123@portfoliocluster.m1sbcxl.mongodb.net/PortfolioDB?retryWrites=true&w=majority
      ACCESS_TOKEN_SECRET: 566dbd15324f60cc381e300319ec38411e2b14232ca02d1850449efcd81bd7ddf9a3fe7ab3b637f18cad0b5ff9e18a4b2f7e45e123c2c4ab31d21f419ee79485
      REFRESH_TOKEN_SECRET: 96b30c75446a7d58177e68d00bbe4c5b0191182a068544dd49e48640ee3d1ba92a48814ce34cddcd23bb8790be27bd3902e29cd2ce7db9bf647ebf2af03bf74e

    strategy:
      matrix:
        node-version: [14.x, 16.x, 18.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - uses: actions/checkout@v3
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    - run: npm i
    - run: npm run test
